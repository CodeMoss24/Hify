# 文档处理管线验证指南

## 功能概述

已实现完整的文档处理数据管线，包括：
1. 文档解析（TXT/MD/PDF）
2. 智能分块（按段落/句子分割）
3. 向量化（使用火山引擎 Embedding）
4. 存储（Qdrant + MySQL）
5. 状态管理

## 验证步骤

### 1. 确认依赖已安装

```bash
# 激活虚拟环境
source venv/bin/activate

# 确认 PyPDF2 已安装
pip install PyPDF2
```

### 2. 确认服务正在运行

```bash
# 确认 Qdrant 容器正在运行
docker ps | grep qdrant

# 确认 FastAPI 后端正在运行
# 应该在 8000 端口有服务
```

### 3. 测试上传文档并验证处理流程

#### 3.1 创建一个测试文本文件

```bash
cat > test_doc.txt << 'EOF'
这是一个用于测试文档处理管线的示例文档。

Hify 是一个可本地部署的 AI Agent 开发平台，面向团队内部 20-50 人使用。

主要功能包括：
- 多模型提供商管理
- Agent 创建与配置
- 对话引擎
- 知识库与 RAG
- 简版工作流
- MCP 工具接入

向量数据库使用 Qdrant，Embedding 模型使用火山引擎的 doubao-embedding-vision，支持 1024 维向量。

文档分块大小为 512 token，重叠 64 token，按段落优先、句子次之的原则进行分割。

测试用的文档内容。
EOF
```

#### 3.2 上传文档

```bash
# 上传到知识库 ID=1
curl -X POST "http://localhost:8000/api/v1/knowledge-bases/1/documents" \
  -F "file=@test_doc.txt"
```

记录返回的文档 ID。

#### 3.3 检查文档状态

```bash
# 立即检查状态（应该是 processing/pending）
curl -X GET "http://localhost:8000/api/v1/knowledge-bases/1/documents?page=1&page_size=20"

# 等待几秒后再次检查（应该是 done）
sleep 5
curl -X GET "http://localhost:8000/api/v1/knowledge-bases/1/documents?page=1&page_size=20"
```

### 4. 验证 Qdrant 中是否有数据

创建一个简单的验证脚本：

```python
#!/usr/bin/env python3
import sys
import os

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from qdrant_client import QdrantClient
from app.common.config import QDRANT_HOST, QDRANT_PORT

# 连接 Qdrant
client = QdrantClient(host=QDRANT_HOST, port=QDRANT_PORT)

# 查看 collection
print("Collections:", [c.name for c in client.get_collections().collections])

# 查看 knowledge_chunks collection 中的数据
collection_name = "knowledge_chunks"
try:
    result = client.scroll(collection_name=collection_name, limit=10)
    points = result[0]
    print(f"\nFound {len(points)} points in {collection_name}")
    for point in points[:3]:
        print(f"\nPoint ID: {point.id}")
        print(f"Payload: {point.payload}")
        print(f"Vector dimension: {len(point.vector)}")
except Exception as e:
    print(f"Error: {e}")
```

## 预期结果

### 文档状态变化

1. 刚上传后：`status: "pending"` 或 `"processing"`
2. 处理完成后：`status: "done"`

### Qdrant 数据

- Collection 名称：`knowledge_chunks`
- 向量维度：1024
- 每个 chunk 的 payload 包含：
  - `document_id`
  - `knowledge_base_id`
  - `chunk_index`
  - `content`

## 分块测试

可以用以下脚本测试分块功能：

```python
#!/usr/bin/env python3
import sys
import os

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from app.knowledge.pipeline import DocumentPipeline

pipeline = DocumentPipeline()

test_text = """这是测试文本。"""

chunks = pipeline._split_chunks(test_text)
print(f"分块数量: {len(chunks)}")
for i, chunk in enumerate(chunks):
    print(f"\nChunk {i}:")
    print(f"  Token估算: {chunk['token_count']}")
    print(f"  内容: {chunk['content']}")
```

## 故障排查

### Qdrant 连接失败

```bash
# 启动 Qdrant（如果需要）
cd /path/to/hify
docker-compose -f docker-compose.dev.yml up -d qdrant
```

### 向量化失败

确认 `.env` 文件中有正确的火山引擎 API Key：

```
ARK_EMBEDDING_API_KEY=your_api_key_here
```

## 已实现的模块

- `app/knowledge/pipeline.py` - 文档处理管线
- 更新了 `app/knowledge/service.py` - 集成异步任务
- 更新了 `app/knowledge/models.py` - 数据模型
