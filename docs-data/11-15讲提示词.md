好的，我帮你从第11讲到第17讲，按“做什么 → 目的 → 提示词”的格式完整整理。

---

# 第11讲—第17讲 全流程提示词总结

---

## 第11讲：基础组件（下）— 前端 UI 设计与前端基础组件

---

### 1. 定风格方向（咨询模式）

**做什么：** 让 Claude 当设计师，根据产品定位和用户画像设计一套完整的 CSS 设计系统。

**目的：** 后端工程师没有设计师的审美，不知道配色怎么选、间距怎么给。把产品是什么、用户是谁、想要什么风格描述清楚，Claude 能给出 80 分以上的设计。

**提示词（直接复制老师的）：**

> Hify 是一个 AI Agent 开发平台，面向技术团队内部使用，主要用户是开发者和技术管理者。界面以管理后台为主——大量的表格、表单、配置页面，加上一个对话交互页面。我想要的视觉风格：浅底 + 科技感点缀。整体用浅色背景保持信息可读性（管理后台表格多，深色底长时间看眼睛累）。但不要太素——侧边栏用深色底，按钮和关键交互元素用亮色，制造科技感和品牌感。色调方向：主色用蓝紫系（科技感强），辅色用青色或薄荷绿（数据/状态指示）。参考 Linear、Supabase 的视觉风格——干净但不无聊，有设计感但不花哨。帮我设计一套完整的设计系统：主色/辅色/背景色阶/文字色阶/圆角/阴影/过渡动效，用 CSS 变量输出。

---

### 2. 改造侧边栏

**做什么：** 把侧边栏从 Element Plus 默认灰蓝改成深色底 + 品牌感设计。

**目的：** 侧边栏是用户打开页面第一眼看到的东西，改造效果最立竿见影。深色侧边栏和浅色内容区形成对比，营造科技感和层次感。

**提示词（直接复制老师的）：**

> 改造 Hify 的侧边栏。要求：背景用深色（接近纯黑但不是纯黑，用 --color-bg-dark），和浅色内容区形成对比。顶部 Logo 区域：显示"Hify"品牌名，用主色渐变文字，下面一行小字"AI Agent Platform"。菜单项：默认白色文字 + 透明背景，hover 时背景微亮（rgba 白色 10% 透明度），选中态左边一条 3px 的主色竖线 + 背景微亮。菜单图标：每个菜单项前加 Element Plus 图标（模型管理用 Setting，Agent 管理用 User，对话用 ChatDotRound）。底部：折叠/展开按钮，版本号。整体要炫酷，有科技感，但不能花哨到影响使用。

---

### 3. 页面整体布局优化

**做什么：** 优化页面整体布局——顶栏、内容区、间距、按钮样式。

**目的：** 让页面有层次感，信息可读性强，操作区域清晰。

**提示词（直接复制老师的）：**

> 优化 Hify 的页面整体布局：顶栏左侧面包屑导航（显示当前页面路径），右侧用户信息区域（头像 + 用户名 placeholder）。内容区背景用 --color-bg-secondary（浅灰），内容卡片用白色背景 + 轻阴影 + 圆角，和背景有层次感。页面标题区域：每个页面顶部有标题 + 描述文字 + 操作按钮区（右侧）。间距统一：页面边距 24px，卡片内边距 20px，元素间距 16px。按钮样式：主要操作用主色渐变按钮（蓝紫渐变），次要操作用白色边框按钮，危险操作用红色。

---

### 4. 一条指令生成五个前端公共组件

**做什么：** 一次性创建 HifyTable、HifyFormDialog、useConfirm、useRequest、notify 五个前端公共组件。

**目的：** 后端工程师不需要在前端组件的实现细节上花太多时间。把每个组件的接口和行为描述清楚，让 AI 一次性生成，以后每个业务页面直接复用这些组件，不需要重复写表格分页、弹窗开关、删除确认这些样板代码。

**提示词（直接复制老师的）：**

> 在 hify-web 中创建以下前端公共组件，放在 src/components/ 目录下。所有组件使用 Vue 3 Composition API + TypeScript + Element Plus：
>
> HifyTable.vue：通用列表页表格组件。Props 接收 columns 配置（label/prop/width/slot）、api 方法（返回 PageResult 格式）、是否显示分页。内部自动管理 loading 状态、分页参数、数据请求。暴露 refresh() 方法供外部调用刷新。空状态显示 Element Plus 的 el-empty。
>
> HifyFormDialog.vue：通用表单弹窗组件。Props 接收 title、width、表单 rules。v-model 控制显示隐藏。内部管理提交 loading、关闭时自动重置表单。暴露 open(data?) 方法，传 data 为编辑模式，不传为新增模式。提交时触发 submit 事件，由父组件处理 API 调用。
>
> src/composables/useConfirm.ts：删除确认 composable。接收确认文案和 API 方法，弹出 ElMessageBox 确认框，确认后调用 API，成功后显示 ElMessage 成功提示，返回 Promise。一行代码完成"确认删除→调接口→提示成功"全流程。
>
> src/composables/useRequest.ts：请求状态管理 composable。接收 API 方法，返回 { data, loading, error, execute }。自动管理三态，避免每个页面写 try-catch-finally 样板代码。
>
> src/utils/notify.ts：统一通知封装。导出 notifySuccess/notifyError/notifyWarning 三个方法，底层调用 ElMessage，统一配置 duration 和样式。
>
> 每个组件要有 TypeScript 类型定义，用泛型支持不同数据类型。组件风格和第一步定的设计系统一致。

---

### 5. Provider 列表页面实现（细节打磨）

**做什么：** 用 HifyTable 和 HifyFormDialog 实现 Provider 列表页，然后来回打磨 UI 细节。

**目的：** 这是前端基础组件落地的第一个完整页面，也是后续所有业务页面的样板。通过反复打磨（间距、颜色、弹窗宽度、hover 效果），训练“哪里不对 + 应该怎样”的判断力。

**提示词（整合版，直接复制给 Claude）：**

> 基于我们已经完成的设计系统 CSS 变量（`design-tokens.css`）和五个前端公共组件（HifyTable、HifyFormDialog、useConfirm、useRequest、notify），现在实现 Provider 列表页面的完整 UI，并做以下细节打磨。
>
> **1. Provider 列表页面**
> 修改 `hify-web/src/views/provider/ProviderView.vue`：
> - 使用 HifyTable 展示表格，列：名称、类型（OpenAI/Claude/Gemini/Ollama）、Base URL、状态（启用/禁用，el-tag）、创建时间、操作（编辑/删除）
> - 页面顶部：标题"模型提供商管理"、描述"管理 AI 模型提供商配置与连接"、右侧"新增提供商"按钮（主色渐变，带 Plus 图标）
> - 用 mock 数据 5 条，类型分布开
> - 点新增/编辑弹出 HifyFormDialog，表单字段：名称、类型（下拉选择）、API Key、Base URL
> - 删除用 useConfirm
>
> **2. 细节打磨（全部做完，不要省）**
> - 表格行高 52px，操作列两个按钮间距 8px
> - 状态列：启用用绿色 el-tag（`type="success"`），禁用用灰色（`type="info"`），不用红色
> - 弹窗宽度 520px，API Key 输入框 password 类型带显示/隐藏切换按钮，Base URL 输入框 placeholder 示例 `https://api.openai.com/v1`，表单 label 右对齐 100px
> - 表格头部背景用 `--bg-elevated`（浅灰），分页器居右对齐上方加分割线，新增按钮带 Plus 图标
> - 表格行 hover 时背景微微变色，编辑按钮 text 类型蓝色，删除按钮 text 类型红色
> - 浏览器宽度小于 1200px 时，侧边栏自动折叠成只显示图标的窄模式，表格隐藏 Base URL 和创建时间两列
>
> 完成后启动前端让我验收，告诉我启动命令和验证方式。

---

## 第12讲：实操课— 工程搭建全流程演示

第12讲是实操课，用录屏把第8-11讲的全过程完整演示一遍，没有新提示词。核心经验总结：任务拆解以“一次能验证”为单位、报错信息要完整给 Claude、UI 打磨用“哪里不对 + 应该怎样”句式、熟能生巧。这些经验已经融入我们后续的实践中。

---

## 第13讲：模型提供商管理— 第一个完整功能的交付闭环

---

### 1. 供应商分析（咨询模式）

**做什么：** 让 Claude 分析主流 LLM 供应商有哪些、API 有什么共性和差异、一期必须支持哪些。

**目的：** 你不了解 LLM 供应商的全貌，让 Claude 帮你梳理，建立全局认知，然后做取舍。

**提示词（直接复制老师的）：**

> Hify 要支持 LLM 模型提供商管理。帮我分析：主流的 LLM 供应商有哪些？它们的 API 有什么共性和差异？哪些是一期必须支持的？

---

### 2. 依赖库分析（咨询模式）

**做什么：** 让 Claude 分析 Python 生态里有没有现成的多 LLM 调用库，是否适合一期使用。

**目的：** 不盲目引入大框架。如果现有依赖库太重、太黑盒、或者和项目定位冲突，就自己封装。

**提示词（Python 版本）：**

> 我在做一个叫 Hify 的项目，技术栈是 Python FastAPI，需要调用多个 LLM 提供商（OpenAI、Anthropic、Ollama 以及 OpenAI 兼容的国内厂商）。Python 生态里有现成的多 LLM 调用库吗？比如 `openai` 官方 SDK、`anthropic` SDK、`langchain`、`llama-index` 等。帮我分析它们的成熟度、优缺点，以及是否适合我的项目一期使用。一期目标是：用最少的依赖、最简的封装，跑通 Provider 管理模块，不引入不必要的框架。

---

### 3. 数据模型设计（咨询模式）

**做什么：** 让 Claude 设计 Provider 模块的数据模型——鉴权信息怎么存、模型列表怎么管理、健康状态怎么表示。

**目的：** 数据怎么存决定了接口怎么设计、前端怎么展示。这是“想清楚”阶段最关键的产出。

**提示词（Python 版本）：**

> 基于上面的供应商分析和依赖库分析，帮我设计 Provider 模块的数据模型。需要考虑：1. 多种供应商鉴权方式的差异怎么统一存储——OpenAI 用 API Key、Anthropic 额外需要 Version Header、Ollama 完全不需要认证；2. 一个供应商下有多个模型怎么管理——展示名和调用时的 model_id 可能不同；3. 供应商健康状态怎么表示——需要高频更新，不能影响业务读性能。技术栈：FastAPI + SQLAlchemy + MySQL 8.x。要遵守 CLAUDE.md 里的数据库规范（表名 `tb_{module}_{entity}`、四个公共字段、索引规则等）。

---

### 4. 数据模型落地：更新 Schema + ORM + DTO

**做什么：** 把 Claude 分析的数据模型设计落实到代码——更新 `db/schema.sql`、创建 ORM 模型和 Pydantic DTO。

**目的：** 设计定稿后立刻落地成代码，不做纸上谈兵。

**提示词（Python 版本）：**

> 按你刚才的数据模型设计，直接写代码。需要产出：1. 更新 `db/schema.sql`，补上 `extra_config`、`status`、`capabilities` 等新字段，建 `tb_provider_health_log` 表；2. 在 `app/provider/models.py` 里创建/更新三个 ORM 模型：`ProviderModel`（增强）、`ModelModel`（新增）、`ProviderHealthLog`（新增）；3. 在 `app/provider/schemas.py` 里创建对应的 Pydantic DTO（请求和响应）；4. 遵守 CLAUDE.md 里的命名规则、字段规范和响应格式。

---

### 5. 连通性测试

**做什么：** 实现连通性测试功能——根据 provider_type 调不同的 API 端点，验证配置是否正确。

**目的：** 用户填完 Provider 配置后，点“测试连接”按钮，后端自动验证能否连通。这是 Provider 模块区别于普通 CRUD 的核心功能。

**提示词（Python 版本）：**

> 在 Provider 模块里实现连通性测试功能。要求：1. 在 `app/provider/service.py` 里给 `ProviderService` 新增 `test_connection(provider_id: int, db: Session)` 方法；2. 根据 `provider_type` 分发到不同的测试逻辑：openai/openai_compatible 调 GET {base_url}/v1/models（Bearer Token 鉴权，超时 10s），anthropic 调 GET {base_url}/v1/models（Header 加 x-api-key 和 anthropic-version，超时 10s），ollama 调 GET {base_url}/api/tags（无需鉴权，超时 10s）；3. 返回 `ConnectionTestResult` 对象（success、latency_ms、model_count、error_message）；4. 在 schemas.py 新增 `ConnectionTestResult`，在 router.py 新增 `POST /providers/{id}/test-connection` 路由。

---

### 6. 连通性测试修复：POST 替代 GET（兼容国内厂商）

**做什么：** 把连通性测试从 `GET /models` 改为 `POST /chat/completions` 发最小请求。

**目的：** 国内厂商（火山引擎、DeepSeek 等）大多不支持 `GET /models` 接口，返回 401/404。改成 POST 发 `max_tokens=1` 的请求，既验证了连通性，又兼容了国内厂商。

**提示词：** 这一步是问题驱动的——发现火山引擎 401 后，让 Claude 自己分析根因并修复，没有提前设计的提示词。

---

### 7. 健康检查定时任务

**做什么：** 后台定时任务，每 60 秒自动探测所有启用的 Provider，健康状态更新到 Redis。

**目的：** 用户不用每次手动点“测试连接”，系统自动监控所有供应商的健康状态。前端展示 Provider 列表时直接读 Redis，用户一眼看到哪些供应商在线、哪些挂了。

**提示词（Python 版本）：**

> 在 Provider 模块里实现健康检查定时任务。要求：1. 在 `app/provider/` 下新建 `health_checker.py`，实现 `HealthChecker` 类：遍历所有 `status='enabled'` 的 Provider，逐个调 `test_connection`；把健康状态写 Redis Hash（`provider:health:{id}`），TTL 120s；连续失败 ≥3 标记 unhealthy；状态切换时写日志到 `tb_provider_health_log`；2. 使用已有的 `async_executor` 异步执行；3. 在 `app/main.py` 的 lifespan 里启动后台协程，每 60s 执行一次；4. 用 logging 记录每次检查的开始、结束、成功数、失败数。

---

### 8. 前端对接：mock 替换为真实 API

**做什么：** 把 Provider 列表页的 mock 数据替换为真实后端 API 调用，新增健康状态列和测试连接按钮。

**目的：** 前后端打通，用户能在浏览器里创建 Provider、测试连接、看到健康状态。

**提示词（Python 版本）：**

> Provider 模块的后端接口已经全部就绪。现在把前端 `ProviderList.vue` 的 mock 数据替换为真实 API 调用。已有基础：`hify-web/src/api/index.ts`（axios 封装）、`HifyTable.vue`、`HifyFormDialog.vue`、`useConfirm.ts`、`useRequest.ts`、`notify.ts`。后端已就绪的接口：GET/POST/PUT/DELETE `/api/v1/providers`、POST `/api/v1/providers/{id}/test-connection`。需要改动：1. 新建 `hify-web/src/api/provider.ts`；2. 修改 `ProviderView.vue`——表格 api 换成 `getProviderList`，弹窗 submit 换成 `createProvider`/`updateProvider`，删除换成 `deleteProvider`，列表加健康状态列和测试连接按钮。

---

## 第14讲：把经验变成 Skill— 让 Claude Code 自动按流程走

---

### 1. 沉淀模块交付 Skill

**做什么：** 把 Provider 模块的完整开发流程（咨询→设计→拆解→执行→前端对接→验收）写成 Skill 文件，让 Claude 以后自动按这个流程走。

**目的：** 后面做 Agent、Chat、Knowledge 等模块，不需要每次都重新描述流程。一句话启动，Claude 自动按 11 步推进，每步等你确认。

**提示词（Python 版本）：**

> 我刚完成了 Hify 项目 Provider 模块的开发，技术栈是 Python FastAPI + SQLAlchemy + Vue 3。整个流程是这样的：1. 先用咨询模式梳理了供应商选型、依赖库分析、数据模型设计；2. 数据模型确定后更新了 `db/schema.sql`；3. 后端按层拆解：models → schemas → interfaces → service → router；4. 每步 curl 验证通过再进下一步；5. 前端对接：创建 API 文件，把 mock 数据源换成真实 API；6. 完整验收。帮我把这个流程沉淀成一个 Skill 文件，放在 `.claude/skills/module-delivery.md`。要求：每一步有明确的产出物和验证方式；关键决策点标注"等待用户确认"；把我们踩过的坑写成注意事项（PageResult 不能继承 ApiResponse、api_key 不能出现在响应 DTO、ORM 对象必须先转 DTO 等）。

---

### 2. 沉淀供应商适配 Skill

**做什么：** 把“接入新 LLM 供应商”的固定流程写成 Skill 文件。

**目的：** 以后加新供应商只需一句话，Claude 自动按 5 步走——分析 API → 实现 Adapter → 在 Factory 注册 → 导出 → 验证。

**提示词（Python 版本）：**

> 我刚用策略模式重构了 Provider 模块的连通性测试。现在的架构是：`ProviderAdapter` 抽象基类 + `ProviderAdapterFactory` 字典映射 + 三个 Adapter 实现。加新供应商的流程非常固定：分析 API → 实现 Adapter → 在 Factory 注册 → 验证。帮我把这个流程沉淀成 Skill 文件，放在 `.claude/skills/provider-adapter.md`。要求：开头写触发方式；每一步有明确的产出物和验证方式；关键决策点标注"等待用户确认"；把我们踩过的坑写成注意事项（国内厂商不支持 GET /v1/models 必须用 POST、Ollama 是唯一用 GET 测试的 Adapter、401/403 返回 success=False 不能抛异常等）。

---

### 3. 策略模式重构 if-else

**做什么：** 把 `ProviderService.test_connection` 里的 `if-else` 按 provider_type 分发逻辑，重构为策略模式（ProviderAdapter 基类 + 三个 Adapter + Factory 字典映射）。

**目的：** 加新供应商只需新建一个 Adapter 类 + 在 Factory 字典里加一行注册，不改任何已有代码。符合“对扩展开放、对修改关闭”的原则。

**提示词（Python 版本）：**

> 第 13 讲 Provider 模块的连通性测试里，`ProviderService.test_connection` 用 `if-else` 按 provider_type 分发不同测试逻辑。现在用策略模式重构这段代码。要求：1. 创建 `app/provider/adapter.py`，定义 `ProviderAdapter` 抽象基类；2. 在 `app/provider/adapters/` 目录下创建 `OpenAiAdapter`、`AnthropicAdapter`、`OllamaAdapter`；3. 创建 `ProviderAdapterFactory`，用字典做映射；4. 重构 `test_connection`，改为 `adapter = factory.get_adapter(provider_type); return await adapter.test_connection(provider)`；5. 加新供应商只需新建 Adapter + 在 Factory 注册一行。

---

## 第15讲：Agent 创建与配置— 复杂业务逻辑的拆解策略

---

### 1. 让 Claude 教你 Agent 概念（领域四问）

**做什么：** 用领域四问让 Claude 解释 Agent 是什么、和普通对话有什么区别、创建 Agent 需要配置哪些东西。

**目的：** 面对一个完全不懂的概念时，先让 Claude 教你，建立全局认知，再做设计。

**提示词（直接复制老师的）：**

> 在 AI 应用平台（比如 Dify）里，Agent 是什么概念？它和普通的对话有什么区别？用户创建一个 Agent 需要配置哪些东西？从产品层面帮我梳理。

---

### 2. 从概念映射到数据结构

**做什么：** 把 Agent 的概念翻译成数据库表结构——需要哪些表、表之间什么关系、System Prompt 用什么类型、模型参数怎么存。

**目的：** 理解一个概念的最高境界，是能把它翻译成数据结构。表结构定了，接口和前端就有了依据。

**提示词（直接复制老师的）：**

> 基于刚才的分析，Agent 在数据库里应该怎么存？需要哪些表？表之间什么关系？特别是：System Prompt 用什么类型、模型参数怎么存、Agent 和工具的多对多关系怎么处理。

---

### 3. 数据模型调整：参数打散存

**做什么：** 把 Agent 的模型参数从 JSON 存改为字段打散存（temperature、max_tokens、max_context_turns 各一列）。

**目的：** Agent 的参数对所有 Agent 都一样，不像 Provider 的鉴权信息那样按类型完全不同。打散存的好处是查询直接、类型约束清晰、不需要多一层 JSON 解析。

**提示词（发给 Claude 的修正指令）：**

> 产品概念梳理没问题。数据模型设计有一个关键点需要调整：**模型参数不要用 JSON 存，改成字段打散存。** 原因：Agent 的参数对所有 Agent 都一样，不像 Provider 的 auth_config 那样按类型完全不同。打散存的好处是查询直接、类型约束清晰。具体改动：删掉 `model_params JSON` 字段，新增 `temperature DECIMAL(3,2)`（默认 0.70）、`max_tokens INT`（默认 2048）、`max_context_turns INT`（默认 10）。`description` 字段可以加，VARCHAR(500)。其他扩展字段一期先不加。

---

### 4. 智能客服场景设计

**做什么：** 用“智能客服”这个具体场景，把 Agent 的抽象数据结构对应到真实业务中——选模型、写 System Prompt、调参数。

**目的：** 设计完了不要立刻写代码，先拿一个真实场景在脑子里跑一遍，验证设计是否合理。每个配置值背后都有产品思考。

**提示词（直接复制老师的）：**

> 基于刚才分析的 Agent 数据模型，帮我设计一个具体的 LLM 应用——智能客服。要求：1. 选模型：GPT-4o，说明为什么选它；2. 写 System Prompt：语气专业友好、回答简洁明了、超出知识范围诚实告知、引导联系人工客服；3. 调参数：temperature 设多少合适？为什么？max_context_turns 设多少？为什么？4. 工具绑定：暂时不绑。把上述配置对应到 Agent 表的字段上，让我看到从概念到数据结构的完整映射。

---

### 5. 拆解 Agent CRUD

**做什么：** 让 Claude 拆解 Agent CRUD 的完整逻辑——从前端请求到数据库落库，创建、查询、更新、删除四个场景每一步怎么走。

**目的：** 复杂逻辑要拆解成有序的步骤，每一步的输入、输出、异常情况都写清楚。需求描述的细致程度决定了代码的正确程度。

**提示词（直接复制老师的）：**

> 帮我拆解 Agent CRUD 的完整逻辑：从前端点保存到数据库落库，中间要经过哪些步骤？把创建、查询、更新、删除四个场景都拆解出来。

---

### 6. 补充 enabled 字段

**做什么：** 给 `tb_agent` 表补上 `enabled` 字段，同步更新 ORM、DTO、Service、前端。

**目的：** 和老师第 15 讲的设计对齐——Agent 表需要 `enabled` 字段让用户手动控制启停。我们在用 Skill 生成时漏了这个字段。

**提示词（Python 版本）：**

> `tb_agent` 表缺少 `enabled` 字段。请同步修改以下文件：1. `db/schema.sql`：新增 `enabled TINYINT(1) NOT NULL DEFAULT 1`；2. `app/agent/models.py`；3. `app/agent/schemas.py`（AgentCreate、AgentUpdate、AgentResponse）；4. `app/agent/service.py`（create_agent、update_agent）；5. 前端 `AgentView.vue`（表格新增状态列，表单新增状态选择下拉）。

---

### 7. AgentView 升级：从模板到定制

**做什么：** 把 Skill 生成的通用 CRUD 页面升级为 Agent 专属配置页面——增加 description、temperature（滑块）、max_tokens、max_context_turns 字段，模型下拉按 Provider 分组。

**目的：** Skill 按 Provider 模板生成的是“能用”的版本，但 Agent 有自己独特的配置项。这一步是在基础版上做精装修。

**提示词（Python 版本）：**

> 升级 `hify-web/src/views/agent/AgentView.vue`，让 Agent 管理页面更符合 Agent 配置的实际需求。表格列新增：description（超长省略）、temperature（展示数值）、max_context_turns、enabled（el-tag）。表单字段新增：description（文本输入）、temperature（el-slider，0~1，步长 0.1）、max_tokens（el-input-number）、max_context_turns（数字输入）。模型下拉升级：按 Provider 分组展示（el-select + el-option-group）。

---

### 8. 补充 CLAUDE.md 规范

**做什么：** 把 Agent 模块踩过的坑补进 CLAUDE.md——关联表更新优先全量替换、语义有歧义的接口要拆开。

**目的：** 做完一个模块不只是交付代码，还要把经验沉淀成规范，下次同类问题不会再犯。

**提示词（Python 版本）：**

> 在 CLAUDE.md 的行为指令部分补充两条新规范：1. **关联表更新优先全量替换**：数据量小时，优先用 DELETE + INSERT 全量替换，逻辑比增量 diff 简单，不易出错。2. **语义有歧义的接口要拆开**：如果同一个接口的某个参数有两种不同语义，拆成两个独立接口，让每个接口的语义唯一、无歧义。

---
